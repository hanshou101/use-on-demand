declare namespace CookieNS {
  interface SwapData {
    swap_token: string;
    swap_expired: string;
    swap_api_key: string;
  }

  interface OtherData {
    Authorization: string;
    lang: string;
  }
}
